# ── CONFIGURACIÓN DEL BOT (GCP) ──────────────────────────────
# Token de Telegram (@BotFather)
BOT_TOKEN: "tu_token_aqui"

# OpenAI
OPENAI_API_KEY: "sk-tu_key_aqui"
OPENAI_MODEL: "gpt-4o-mini"

# Control de acceso (IDs de Telegram separados por comas)
AUTHORIZED_USERS: "12345678,87654321"

# Google Cloud
GCP_PROJECT_ID: "mi-facturador-bot-01"
GCS_BUCKET_NAME: "facturas-erp-storage"

# Configuración del servidor (para Cloud Run)
WEBHOOK_URL: "https://tudominio.a.run.app"
